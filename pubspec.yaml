name: call_agora_lock
description: "A new Flutter project."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.6.1


dependencies:
  flutter:
    sdk: flutter


  cupertino_icons: ^1.0.8
  firebase_core:
  firebase_messaging:
  flutter_local_notifications:
  get: ^4.6.6
  shared_preferences:
  firebase_analytics:
  agora_rtc_engine:
  audioplayers:
  permission_handler:
  workmanager:

dev_dependencies:
  flutter_test:
    sdk: flutter


  flutter_lints: ^5.0.0


flutter:


  uses-material-design: true
assets:
  - assets/
  - assets/sounds/